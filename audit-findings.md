# บันทึกผลตรวจสอบเว็บไซต์

## ผลการตรวจสอบ

เว็บไซต์เป็น React bundle แบบ static ที่มีหน้าเข้าสู่ระบบและโมดูล POS, สินค้า, สต๊อก, แคตตาล็อก, ประวัติการขาย, รายงาน, พนักงาน และการตั้งค่า โดยมีตัวเลือกภาษาไทย อังกฤษ พม่า และลาว และมีการเชื่อมต่อ Supabase รวมอยู่ใน bundle

ไฟล์ต้นฉบับมีปัญหาการอ้างอิงทรัพยากรหลัก: `index.html` เรียก ` /assets/index-BNz1h94P.js` และ ` /assets/index-3xhHjJ1j.css` แต่ไฟล์ที่แนบมาอยู่ที่ root จึงมีความเสี่ยงสูงที่หน้าเว็บจะโหลดเป็นหน้าว่างเมื่อ deploy โดยไม่มีโฟลเดอร์ `assets`

นอกจากนี้ยังมีการอ้างอิง `/favicon.svg` และ `/og-cover.svg` แต่ไม่มีไฟล์ดังกล่าวในชุดต้นฉบับ จึงจัดทำไฟล์ SVG ให้แล้วในเวอร์ชันแก้ไข

## การแก้ไขที่ดำเนินการ

1. จัด JavaScript และ CSS เข้าโฟลเดอร์ `assets/` ให้ตรงกับ path ที่หน้าเว็บเรียกใช้งาน
2. ปรับ path ของ JavaScript และ CSS ใน `index.html` เป็นแบบ relative (`./assets/...`) เพื่อให้ทำงานได้ยืดหยุ่นขึ้นบน static hosting
3. เพิ่ม `favicon.svg` และ `og-cover.svg`
4. เพิ่ม metadata `og:locale` และ `robots`
5. คงไฟล์ bundle ต้นฉบับไว้ที่ root ของแพ็กเกจแก้ไขเพื่อความเข้ากันได้ย้อนหลัง

## ผลการทดสอบ

เปิดหน้าเว็บผ่าน local static server แล้วพบว่าหน้าเข้าสู่ระบบแสดงผลได้จริง ไม่เป็นหน้าว่าง และมีตัวเลือกภาษาไทย อังกฤษ พม่า และลาว

ตรวจสอบ HTTP status ของทรัพยากรสำคัญแล้วได้ผลดังนี้:

| ทรัพยากร | สถานะ | ประเภท |
|---|---:|---|
| `./assets/index-BNz1h94P.js` | 200 | JavaScript |
| `./assets/index-3xhHjJ1j.css` | 200 | CSS |
| `./favicon.svg` | 200 | SVG |
| `./og-cover.svg` | 200 | SVG |

## ข้อจำกัดที่ควรทราบ

ชุดไฟล์ที่ได้รับเป็นไฟล์ build แล้ว ไม่ใช่ source project จึงไม่ควรแก้ตรรกะภายในแอปโดยตรง เพราะจะทำให้ดูแลต่อยากและอาจทำให้ source map หรือ build pipeline ไม่สอดคล้องกัน หากต้องการปรับฟังก์ชันเชิงลึก ควรขอ source project ของ React/TypeScript เพิ่มเติม

ระบบมี URL Supabase แบบ hard-coded อยู่ใน bundle และหน้า login ต้องใช้บัญชีที่มีอยู่จริง จึงทดสอบต่อถึงขั้นล็อกอินหรือ CRUD ในฐานข้อมูลไม่ได้โดยไม่ใช้ข้อมูลรับรองของผู้ใช้

การทดสอบ interaction เพิ่มเติมพบว่าปุ่ม `English` เปลี่ยนข้อความของหน้าเป็นภาษาอังกฤษได้ และปุ่ม `မြန်မာ` เปลี่ยนข้อความเป็นภาษาพม่าได้ พร้อมเปลี่ยน label และ placeholder ของฟอร์มตามภาษาโดยไม่เกิดข้อผิดพลาดที่มองเห็นได้

ก่อนส่งมอบได้ลบ bundle ที่ซ้ำจาก root ออก เหลือโครงสร้าง static site ที่สะอาด: `index.html`, `assets/`, `_redirects`, `favicon.svg`, `og-cover.svg` และ `llms.txt` พร้อมไฟล์บันทึกผลตรวจสอบ
